#include <stdio.h>
#include <math.h>

int main ()
{
  printf ( "ceil of 2.3 is %.1f\n", ceil((double)1/1000000000) );
  printf ( "ceil of 3.8 is %.1f\n", ceil() );
  printf ( "ceil of -2.3 is %.1f\n", ceil(-2.3) );
  printf ( "ceil of -3.8 is %.1f\n", ceil(-3.8) );
  return 0;
}
